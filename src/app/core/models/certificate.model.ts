export interface Certificate {
  id: string;
  personName: string;
  documentNumber: string;
  accountBank: string;
  accountType: string;
  accountNumber: string;
  signerName: string;
  signerRole: string;
  createdAt: string;
}
